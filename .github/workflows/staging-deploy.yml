
name: Staging Deployment


on:
  push:
    branches:
      - dev
  workflow_dispatch:


jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo test
        run: echo "Staging workflow working"

